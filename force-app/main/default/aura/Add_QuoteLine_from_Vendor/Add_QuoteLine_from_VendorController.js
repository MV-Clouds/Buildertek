({
    doInit : function(component, event, helper) {
        console.log('Add_QuoteLine_from_VendorController.doInit');
        var QuoteId = component.get("v.quoteId");
        console.log('QuoteId: ' + QuoteId);
    },
})
